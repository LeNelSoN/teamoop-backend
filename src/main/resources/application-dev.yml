spring:
  datasource:
    url: jdbc:postgresql://localhost:1245/teamoopdb
    username: teamoop
    password: teamoop
  liquibase:
    change-log: classpath:db/changelog/master.xml
    enabled: true
    contexts: dev
  jpa:
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    show-sql: true
    hibernate:
      ddl-auto: none

redis:
  host: localhost
  port: 6379
  password: password
  ttl:
    duration: 10m